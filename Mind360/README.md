# Mind360

Mind360 is a generalized Structured Memory Graph (SMG) SDK for agents.

## Architecture

Input -> Parser -> Normalizer -> Node Generator -> Relationship Builder -> SMG Store (Neo4j)

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Configure Neo4j connection in `.env` (to be created)
